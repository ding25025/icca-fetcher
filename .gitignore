# ---- 機敏設定：含明文密碼與內網位址，絕對不要進版控 ----
databases.config.json
sink.config.json
ring.config.json
ring_config.json
*.local.json
.env

# ---- 執行結果：含病歷號，一律不進版控 ----
# vitals.js / neuro.js 的預設檔名是 vitals_{ts}.json、neuro_{ts}.json，
# 用 --out 自訂檔名時要自己確認有沒有被這裡蓋到。
icca-data.json
icca-vitals.json
vitals_*.json
neuro_*.json
icca-ring-latest.json
ring-result.json
latest.json

# ---- 相依套件 ----
node_modules/
__pycache__/
*.pyc

# ---- 編輯器 / 系統 ----
.vscode/
.idea/
Thumbs.db
.DS_Store

# 表號錨點快取（純快取，刪掉只會讓下次重新掃描）
.ring-anchors.json

# ---- 文件只維護 HTML；PDF 是匯出的產物，不進版控也不要再產 ----
*.pdf
